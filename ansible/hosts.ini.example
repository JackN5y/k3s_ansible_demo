# Copy this file to hosts.ini and adjust to your environment.
#
# --- Option A: run directly on the local machine ---
[k3s_servers]
localhost ansible_connection=local ansible_python_interpreter=/usr/bin/python3

# --- Option B: run against a remote machine ---
# [k3s_servers]
# 192.168.1.10 ansible_user=ubuntu ansible_become=true ansible_ssh_private_key_file=~/.ssh/id_rsa
